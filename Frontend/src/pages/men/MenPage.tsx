import { CatalogView } from '../../widgets/catalog/CatalogView';

export default function MenPage() {
  return (
    <CatalogView
      gender="MEN"
      title="Men"
      description="Shirts, tees and jackets built from durable, natural fabrics. Filter by size, colour and price to narrow the edit."
    />
  );
}
